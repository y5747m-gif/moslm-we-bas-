<?xml version="1.0" encoding="utf-8"?>
<!--
  Fajr Alarm — permissions rationale (shown to the user before requesting):
  - CAMERA: capture the 3 wake-up proof photos inside the ringing screen only.
  - POST_NOTIFICATIONS: show the full-screen ringing notification with sound/vibration.
  - SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM: ring exactly on time.
  - RECEIVE_BOOT_COMPLETED: reschedule the alarm after reboot (user-enabled only).
  - VIBRATE / WAKE_LOCK: vibration + keep CPU awake while ringing.
  - FOREGROUND_SERVICE (+ MEDIA_PLAYBACK type): play the ringing sound reliably.
  - USE_FULL_SCREEN_INTENT: show the ringing screen over the lock screen.
  - REQUEST_IGNORE_BATTERY_OPTIMIZATIONS: ask (via settings screen) to keep alarm reliable on aggressive OEMs.
  NOTE: No INTERNET permission on purpose — photos can never leave the device.
  NOTE: No notification-listener / BIND_* permissions — we never read notifications.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FajrAlarm">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen ringing + 3-photo challenge screen -->
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.FajrAlarm.Alarm" />

        <service
            android:name=".AlarmForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver android:name=".AlarmReceiver" android:exported="false" />
        <receiver android:name=".SnoozeReceiver" android:exported="false" />

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
